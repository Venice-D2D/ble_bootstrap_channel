name: ble_bootstrap_channel
description: Transmit metadata between devices through Bluetooth Low Energy.
version: 0.0.1
homepage: https://github.com/Venice-D2D/ble_bootstrap_channel
publish_to: none

environment:
  sdk: '>=3.9.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  bluetooth_low_energy: ^6.2.1
  flutter:
    sdk: flutter
  venice_core:
    git:
      url: https://github.com/Venice-D2D/venice_core.git
      tag_pattern: v{{version}}
    version: 0.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
