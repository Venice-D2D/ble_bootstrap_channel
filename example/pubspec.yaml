name: example
description: Example app showcasing ble_bootstrap_channel capabilities.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.6 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  ble_bootstrap_channel:
    path: ../
  venice_core:
    git: https://github.com/Venice-D2D/venice_core.git
  cupertino_icons: ^1.0.2
  fluttertoast: ^8.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
